/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com', 'm.media-amazon.com', 'lh3.googleusercontent.com'], // permitimos imágenes
    },
};

module.exports = nextConfig;